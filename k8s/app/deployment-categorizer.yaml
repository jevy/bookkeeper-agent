apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookkeeper
  labels:
    app: bookkeeper
    component: categorizer
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bookkeeper
      component: categorizer
  template:
    metadata:
      labels:
        app: bookkeeper
        component: categorizer
    spec:
      containers:
        - name: categorizer
          image: ghcr.io/jevy/bookkeeper-agent:0.8.3
          args: ["categorizer"]
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "redpanda.apps.svc.cluster.local:9093"
            - name: SCHEMA_REGISTRY_URL
              value: "http://redpanda.apps.svc.cluster.local:8081"
            - name: GOOGLE_SHEET_ID
              valueFrom:
                secretKeyRef:
                  name: bookkeeper
                  key: GOOGLE_SHEET_ID
            - name: GOOGLE_CREDENTIALS_JSON
              valueFrom:
                secretKeyRef:
                  name: bookkeeper
                  key: GOOGLE_CREDENTIALS_JSON
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: bookkeeper
                  key: OPENROUTER_API_KEY
            - name: BRAVE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: bookkeeper
                  key: BRAVE_API_KEY
            - name: ADDITIONAL_CONTEXT_PROMPT
              valueFrom:
                secretKeyRef:
                  name: bookkeeper
                  key: ADDITIONAL_CONTEXT_PROMPT
          ports:
            - containerPort: 9091
              name: metrics
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9091
            initialDelaySeconds: 120
            periodSeconds: 60
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
